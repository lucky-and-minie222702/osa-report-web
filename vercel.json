{
	"version": 2,
	"builds": [
		{
			"src": "index.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/frontend/(.*)",
			"dest": "/"
		},
		{
			"src": "/uploads/(.*)",
			"dest": "/uploads"
		}
	]
}
